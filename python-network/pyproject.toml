[project]
name = "python-network"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "confluent-kafka>=2.14.2",
    "cryptography>=48.0.0",
    "protobuf>=7.35.0",
]
